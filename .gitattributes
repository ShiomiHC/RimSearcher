# 输出快照基线必须在工作区里也保持 LF。
#
# 本仓的 core.autocrlf=true：源码在仓库里存 LF、签出时转 CRLF，这对 .cs 无害。但基线是
# **被逐字比较的数据**——工具输出的行尾一律是 LF（ToolResult 构造时统一收口），基线被签出成
# CRLF 之后每一份都对不上，于是这道闸会在任何一次干净 clone 或 checkout 之后全红，而红的原因
# 与被测代码无关。
#
# 用 `text eol=lf` 而不是 `-text`：前者仍是文本文件（diff 与 merge 照常工作，基线要被人读、被
# review 审），只是工作区行尾钉死成 LF。
Sources/RimSearcher.Tests/Snapshots/**/*.txt text eol=lf
