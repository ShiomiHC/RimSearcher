$ rimsearcher fields ThingComp
exit 1
--- stdout ---
No def type named 'ThingComp' in this snapshot. 'rimsearcher list' with no def type lists them all.
'ThingComp' is a type in the decompiled source all the same — vanilla/Verse/ThingComp.cs. Only a def type gets a def database, so this command has nothing of its own to read for it, while the source does: 'rimsearcher read ThingComp.cs --outline' names every field, property and method with its line range.
--- stderr ---
