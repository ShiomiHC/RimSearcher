$ rimsearcher fields ThingDef --limit 3 --offset 3
exit 0
--- stdout ---
3 of 6 field paths, starting at 4; that is the last page.

path                         defs
comps[0].props.energyMax     1
ingestible.foodType          1
projectile.damageAmountBase  1

Counted over indexed field paths only: 1 def of the same def types lost fields at export time and could belong here without showing up. 'rimsearcher snapshot truncated' lists them.
--- stderr ---
