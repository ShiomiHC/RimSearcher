$ rimsearcher fields ThingDef --limit 3 --offset 3
exit 0
--- stdout ---
3 of 16 field paths, starting at 4; pass --offset 6 for the next page, or --limit all for every one at once, or narrow with --path <text>.

path                defs
soundPickup         9
thingClass          6
comps[0].compClass  2

Counted over indexed field paths only: the def types that carry this path (ThingDef) also hold 1 def that lost fields at export time and could belong here without showing up. 'rimsearcher snapshot truncated --type ThingDef' lists them.
--- stderr ---
