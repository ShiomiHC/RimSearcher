$ rimsearcher values noSuchField
exit 1
--- stdout ---
No def in this snapshot has a field path ending in 'noSuchField'. 'rimsearcher fields <DefType>' lists the paths a type actually has, and 'rimsearcher find --value <text>' finds which path holds a value you already know.
Two things keep a field out of this index without any sign here: a value that was null on every def, and a field the game marks as an unsaved runtime cache. A third, hitting the per-def field cap, does leave a sign — 'rimsearcher snapshot truncated' lists those defs. The runtime type of a nested Class="..." object is indexed as '<path>.Class' for list elements only (<li Class="...">), so 'rimsearcher find Class <ClassName>' reaches those. A single field that picks a class — GenStepDef.genStep, ThinkTreeDef.thinkRoot — is not in this snapshot at all: it was written by exporter 0.2.0, before that case entered the index, and a zero from that query is not evidence about it. Re-export to reach it. So this says no indexed value sits at that path — not that no such field exists. 'rimsearcher code-search' reads the class declaration, which does say.
--- stderr ---
