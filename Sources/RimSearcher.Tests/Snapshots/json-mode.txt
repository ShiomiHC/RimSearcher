$ rimsearcher get Apparel_ShieldBelt --limit 2 --json
exit 0
--- stdout ---
{
  "notes": [
    {
      "kind": "truncation",
      "text": "12 fields, showing the first 2.",
      "shown": 2,
      "total": 12
    },
    {
      "kind": "filter",
      "text": "Not listed: 1 field whose value matches the declaring type's own default; --defaults lists them. That match is not evidence that nothing wrote them: a def whose XML writes the default value and a def that never mentions the field are byte-for-byte identical here. The snapshot holds 13 field paths for this def; a null-valued field never entered the index and is in neither count. Nothing below shows any field of these list entries, which the def has all the same: comps[0], comps[1], statBases[0], and 1 more."
    },
    {
      "kind": "advisory",
      "text": "Values that most of the 9 ThingDefs in this snapshot also carry, so their 'code_default' is not this def having made a choice — the count in brackets: soundDrop (9), soundImpactDefault (9)."
    },
    {
      "kind": "next_step",
      "text": "The game also prices this thing. Its market value, cost to make and work amount are computed, not stored, so no field above holds them — 'rimsearcher economy Apparel_ShieldBelt' is the only road to those numbers."
    },
    {
      "kind": "count",
      "text": "1 translation.",
      "shown": 1,
      "total": 1
    },
    {
      "kind": "boundary",
      "text": "This snapshot never scanned the language files on disk, so every row here is one the game actually had: the absence of an 'on disk' row is not evidence that no installed mod translates it. No 'mod_roots' is configured on this machine, so there is nowhere to scan: set it in the config file and import again to measure that layer."
    }
  ],
  "defs": [
    {
      "def": {
        "def_name": "Apparel_ShieldBelt",
        "def_type": "ThingDef",
        "label": "shield belt",
        "description": "",
        "class": "Verse.ThingDef",
        "mod": "ludeon.rimworld",
        "source": "Apparel_Belts.xml",
        "xml": "not indexed (exporter 0.2.0)"
      },
      "fields": [
        {
          "path": "soundImpactDefault",
          "value": "BulletImpact_Ground",
          "code_default": "no"
        },
        {
          "path": "soundDrop",
          "value": "Standard_Drop",
          "code_default": "no"
        }
      ],
      "translations": [
        {
          "path": "label",
          "translated": "护盾腰带",
          "original": "shield belt",
          "language": "ChineseSimplified",
          "origin": "in pack"
        }
      ]
    }
  ]
}
--- stderr ---
