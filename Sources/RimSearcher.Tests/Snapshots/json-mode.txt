$ rimsearcher get Apparel_ShieldBelt --limit 2 --json
exit 0
--- stdout ---
{
  "notes": [
    {
      "kind": "truncation",
      "text": "Showing 2 of 11 fields."
    },
    {
      "kind": "filter",
      "text": "Not listed: 1 field whose value matches the declaring type's own default; --defaults lists them. That match is not evidence that nothing wrote them. The snapshot holds 12 field paths for this def; a null-valued field never entered the index and is in neither count. Nothing below shows any field of these list entries, which the def has all the same: comps[0], statBases[0], statBases[1] — so the lists run longer than they look here."
    },
    {
      "kind": "advisory",
      "text": "Values that most of the 9 ThingDefs in this snapshot also carry, so their 'code_default' is not this def having made a choice — the count in brackets: soundDrop (9), soundImpactDefault (9). Only rows whose 'code_default' is no were compared: a yes is not evidence that nothing wrote the value — it only says the value matches what a fresh instance of the declaring type carries."
    },
    {
      "kind": "count",
      "text": "1 translation."
    },
    {
      "kind": "boundary",
      "text": "This snapshot never scanned the language files on disk, so every row here is one the game actually had: the absence of an 'on disk' row is not evidence that no installed mod translates it. No 'mod_roots' is configured on this machine, so there is nowhere to scan: set it in the config file and import again to measure that layer."
    }
  ],
  "defs": [
    {
      "def": {
        "def_name": "Apparel_ShieldBelt",
        "def_type": "ThingDef",
        "label": "shield belt",
        "description": "",
        "class": "Verse.ThingDef",
        "mod": "ludeon.rimworld",
        "source": "Apparel_Belts.xml"
      },
      "fields": [
        {
          "path": "soundImpactDefault",
          "value": "BulletImpact_Ground",
          "code_default": "no"
        },
        {
          "path": "soundDrop",
          "value": "Standard_Drop",
          "code_default": "no"
        }
      ],
      "translations": [
        {
          "path": "label",
          "translated": "护盾腰带",
          "original": "shield belt",
          "language": "ChineseSimplified",
          "origin": "in effect"
        }
      ]
    }
  ]
}
--- stderr ---
