$ rimsearcher keyed NoSuchUiKey
exit 1
--- stdout ---
No keyed translation matches 'NoSuchUiKey'.
Two things are outside this layer by construction. A def's own label or description is translated through DefInjected, not through a key: 'rimsearcher search NoSuchUiKey' covers those. And a key the code assembles at runtime ('"Stat_" + x') exists in the language files but appears in no source line as a literal, so searching the code for it finds nothing even though this command can still show it by name.
--- stderr ---
