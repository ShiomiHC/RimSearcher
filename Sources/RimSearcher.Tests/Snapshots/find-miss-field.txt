$ rimsearcher find noSuchField x
exit 1
--- stdout ---
No def in this snapshot has a field path ending in 'noSuchField'. 'rimsearcher fields <DefType> --path <text>' lists the paths that a def type actually has, and 'rimsearcher find --value x' finds which field holds that value.
A field whose value is null on every def never entered this index. The runtime type of a nested <li Class="..."> object is indexed as '<path>.Class', so 'rimsearcher find Class <ClassName>' is the query that reaches it. So this says no indexed value sits at that path — not that no such field exists. 'rimsearcher code-search' reads the class declaration, which does say.
--- stderr ---
