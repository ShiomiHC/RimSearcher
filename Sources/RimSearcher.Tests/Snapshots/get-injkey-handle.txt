$ rimsearcher get ObservedLayingCorpse --path stages.observed_corpse.label --fixture-injkey
exit 0
--- stdout ---
No field path contains 'stages.observed_corpse.label'; the def does have 4 fields. Drop --path-contains to see them.
No indexed value sits at that path. 'rimsearcher code-search "[\w<>,\[\] ]+ label;"' finds the declaration, which does say. Two things keep a field out of this index without any sign here: a value that was null on every def, and a field the game marks as an unsaved runtime cache.

def_name     ObservedLayingCorpse
def_type     HediffDef
label        observed laying corpse
class        Verse.HediffDef
declared_in  ludeon.rimworld
source       Thoughts_Memory.xml
1 translation within --path-contains stages.observed_corpse.label.

path             key                           translated  original         language           origin
stages[0].label  stages.observed_corpse.label  看到了尸体  observed corpse  ChineseSimplified  in effect

layer              state         next
disk_translations  unconfigured  set 'mod_roots' in the config file, then rimsearcher snapshot import injkey.rsx.jsonl.gz --name injkey
--- stderr ---
