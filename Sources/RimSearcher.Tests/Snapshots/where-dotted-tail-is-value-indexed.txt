$ rimsearcher where statBases[0].MarketValue
exit 1
--- stdout ---
No def in this snapshot has a field path ending in 'statBases[0].MarketValue'. 'rimsearcher fields <DefType> --path-contains <text>' lists the paths that a def type actually has.
  - 'MarketValue' is a value in this snapshot, not a field under 'statBases[0]': it sits on statBases[].stat. The query that asks it is 'rimsearcher where statBases[].stat --value MarketValue'.
  - No indexed value sits at that path. 'rimsearcher code-search "[\w<>,\[\] ]+ MarketValue;"' finds the declaration, which does say.
--- stderr ---
