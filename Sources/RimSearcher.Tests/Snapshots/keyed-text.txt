$ rimsearcher keyed 没有电力
exit 0
--- stdout ---
1 key.

key               translated  original  origin     placeholder  mod  source
CannotUseNoPower  没有电力    No power  in effect  no                Misc.xml:12
To find the code that prints it, search for the key as a literal: 'rimsearcher code-search "\"CannotUseNoPower\""'. Most call sites write the key inline, but not all of them do — a key assembled from parts will not appear that way.

This snapshot never scanned the language files on disk, so every row here is one the game actually had: the absence of an 'on disk' row is not evidence that no installed mod translates it. No 'mod_roots' is configured on this machine, so there is nowhere to scan: set it in the config file and import again to measure that layer.
--- stderr ---
