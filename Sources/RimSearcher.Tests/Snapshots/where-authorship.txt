$ rimsearcher where compClass TestMod.CompBoltedOn
exit 0
--- stdout ---
2 defs.
'TestMod.CompBoltedOn' sits on 1 def declared in official mods; the 'xml' column of 'rimsearcher get <defName> --defaults' says whether their XML wrote it.

Defs that may be missing from this answer, their export cut short — every def type that uses this path at all
def_type  defs_cut_short
ThingDef  1
verify: rimsearcher snapshot truncated --type ThingDef

Same in every row, not repeated below: def_type=ThingDef, path=comps[1].compClass, value=TestMod.CompBoltedOn, code_default=no.
def_name            declared_in
Apparel_ShieldBelt  ludeon.rimworld
TestModGun          test.mod
--- stderr ---
