$ rimsearcher get Apparel_ShieldBelt --path-contains MarketValue
exit 0
--- stdout ---
No field path contains 'MarketValue'; the def does have 13 fields. Drop --path-contains to see them.

asked        state           next
MarketValue  value-not-path  rimsearcher where --value MarketValue

def_name     Apparel_ShieldBelt
def_type     ThingDef
label        shield belt
class        Verse.ThingDef
declared_in  ludeon.rimworld
source       Apparel_Belts.xml
xml          not indexed (exporter 0.2.0)
Market value, cost to make and work amount are not fields: 'rimsearcher economy Apparel_ShieldBelt' has them.
Filtered away: 1 translation this def does have, none of whose paths contain 'MarketValue'. Their paths are the game's injection keys as written, and this snapshot predates the pass that brings them onto the field paths' grammar: where a field path has '[0]', a key has either the number or a name taken from that element's own label, and neither spelling contains the other. Ask for the field name on its own — the part before the subscript — to reach both tables, or re-export to get one grammar.
--- stderr ---
