$ rimsearcher fields ThingDef --exact-path zzzNoSuchField
exit 1
--- stdout ---
'ThingDef' has field paths, but none is 'zzzNoSuchField'. No def of 'ThingDef' holds 'zzzNoSuchField' as a value either. Drop --exact-path to see them all.
No indexed value sits at that path. 'rimsearcher code-search "[\w<>,\[\] ]+ zzzNoSuchField;"' finds the declaration, which does say. This snapshot (exporter 0.2.0) does not list the fields a type can have, so it cannot tell a field that is null on every def from one the type does not have. The runtime type of a nested Class="..." object is indexed as '<path>.Class' for list elements only (<li Class="...">), so 'rimsearcher where Class <ClassName>' reaches those. A single field that picks a class — GenStepDef.genStep, ThinkTreeDef.thinkRoot — is not in this snapshot at all: it was written by exporter 0.2.0, before that case entered the index. Re-export to reach it.
--- stderr ---
