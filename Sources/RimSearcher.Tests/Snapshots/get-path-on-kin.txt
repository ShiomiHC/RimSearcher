$ rimsearcher get Apparel_ShieldBelt --path-contains ingestible
exit 0
--- stdout ---
No field path contains 'ingestible'; the def does have 13 fields. Drop --path-contains to see them. Other defs of this type do have it: 1 def across 2 field paths; a field that is null on a def never entered the index. 'rimsearcher fields ThingDef --path-contains ingestible' names those paths.

def_name     Apparel_ShieldBelt
def_type     ThingDef
label        shield belt
class        Verse.ThingDef
mod          ludeon.rimworld
source       Apparel_Belts.xml
xml          not indexed (exporter 0.2.0)
The game also prices this thing. Its market value, cost to make and work amount are computed, not stored, so no field above holds them — 'rimsearcher economy Apparel_ShieldBelt' is the only road to those numbers.
Filtered away: 1 translation this def does have, none of whose paths contain 'ingestible'. Their paths are the game's injection keys as written, and this snapshot predates the pass that brings them onto the field paths' grammar: where a field path has '[0]', a key has either the number or a name taken from that element's own label, and neither spelling contains the other. Ask for the field name on its own — the part before the subscript — to reach both tables, or re-export to get one grammar.
--- stderr ---
