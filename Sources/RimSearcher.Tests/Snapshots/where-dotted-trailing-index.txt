$ rimsearcher where stuffProps.categories
exit 0
--- stdout ---
2 matches.
  - Those 2 matches come from 1 def: a def that matches on more than one path has a row for each, so the def_name column repeats.
  - Nothing sits at 'stuffProps.categories' as written — indexed paths carry the list index, so this ran as stuffProps.categories[] (2). That shape goes back in with --exact-path to pin it.

Defs that may be missing from this answer, their export cut short — every def type that uses this path at all
def_type  defs_cut_short
ThingDef  1
verify: rimsearcher snapshot truncated --type ThingDef

Same in every row, not repeated below: def_type=ThingDef, code_default=no, mod=ludeon.rimworld.
def_name    path                      value
FoamPopper  stuffProps.categories[0]  Metallic
FoamPopper  stuffProps.categories[1]  Woody
--- stderr ---
