## Def: ZzWidgetChild
Type: ZzWidgetDef (C# class not indexed)
Source: vanilla
File: `<ROOT>/Core/ZzChain.xml`
Inheritance chain: ZzWidgetChild <- ZzWidgetBase — the XML below is these merged together, so it is not the content of the `File:` path above, which holds only this def's own un-merged lines.

**Resolved XML** (4 lines; mod PatchOperations are not applied, so a mod patch against this def is not reflected below):
```xml
<ZzWidgetDef>
  <defName>ZzWidgetChild</defName>
  <label>base widget</label>
</ZzWidgetDef>
```