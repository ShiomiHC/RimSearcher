$ rimsearcher where stat zzznope --exact-path
exit 1
--- stdout ---
No field path is exactly 'stat', and no path ending in it is set to 'zzznope' either. Those paths: statBases[].stat (3), statFactors[].stat (1). Any one of those goes straight back into --exact-path, where '[]' stands for any index, but the value has to change too.
--- stderr ---
