$ rimsearcher get Bullet_Revolver bullet_revolver
exit 0
--- stdout ---
Given more than once, printed once: bullet_revolver.
  - 7 fields.
  - Not listed: 1 field whose value matches the declaring type's own default (--defaults lists them). The snapshot holds 8 field paths for this def; a null-valued field never entered the index. The 'xml' column refers to the XML read before patches ran.
  - The exporter stopped short on this def: at least 3 fields were dropped at export time for depth or size. Added to the 8 paths that did get indexed, that is at least 11 field paths on this def.

def_name       Bullet_Revolver
def_type       ThingDef
label          revolver bullet
class          Verse.ThingDef
declared_in    ludeon.rimworld
source         Projectiles_Guns.xml
inherits_from  BaseBullet (see 'rimsearcher inherit Bullet_Revolver')

path                         value                code_default  xml
soundImpactDefault           BulletImpact_Ground  no            not-written
soundDrop                    Standard_Drop        no            here
soundPickup                  Standard_Pickup      no            here
soundInteract                Standard_Pickup      no            here
thingClass                   RimWorld.Bullet      no            here
projectile.damageAmountBase  12                   no            here
projectile.speed             70                   unknown       not-written
Values that most of the 9 ThingDefs in this snapshot also carry — the count in brackets: soundDrop (9), soundImpactDefault (9), soundPickup (9), soundInteract (8).
--- stderr ---
