$ rimsearcher get Apparel_ShieldBelt --path-contains comps[].props
exit 0
--- stdout ---
Matching 'comps[].props': 2 fields, out of 13 fields on the def.

def_name     Apparel_ShieldBelt
def_type     ThingDef
label        shield belt
class        Verse.ThingDef
mod          ludeon.rimworld
source       Apparel_Belts.xml
xml          not indexed (exporter 0.2.0)

Same in every row, not repeated below: code_default=no.
path                                value
comps[0].props.energyMax            0.5
comps[0].props.energyLossPerDamage  0.033
No value above with 'code_default'=no is one that most of the 9 ThingDefs in this snapshot also carry.
  - The game also prices this thing. Its market value, cost to make and work amount are computed, not stored, so no field above holds them — 'rimsearcher economy Apparel_ShieldBelt' is the only road to those numbers.
  - Filtered away: 1 translation this def does have, none of whose paths contain 'comps[].props'. Their paths are the game's injection keys as written, and this snapshot predates the pass that brings them onto the field paths' grammar: where a field path has '[0]', a key has either the number or a name taken from that element's own label, and neither spelling contains the other. Ask for the field name on its own — the part before the subscript — to reach both tables, or re-export to get one grammar.
--- stderr ---
