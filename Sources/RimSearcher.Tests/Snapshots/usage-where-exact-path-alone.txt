$ rimsearcher where --value Bloomstone --exact-path
exit 2
--- stdout ---
--- stderr ---
--exact-path pins the field path given as an argument, and this call gives none. Pass the path ('rimsearcher where statBases[].stat MarketValue --exact-path'), or drop the switch. To narrow which paths are searched without naming one, --path-contains takes a fragment.
