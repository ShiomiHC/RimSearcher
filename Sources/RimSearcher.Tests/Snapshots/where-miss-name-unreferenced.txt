$ rimsearcher where Firefoam
exit 4
--- stdout ---
No def in this snapshot has a field path ending in 'Firefoam'. 'rimsearcher fields <DefType> --path-contains <text>' lists the paths that a def type actually has.

Where the name does turn up:
name      is   in                                                       next
Firefoam  def  ThingDef in ludeon.rimworld, StatDef in ludeon.rimworld  rimsearcher get Firefoam
No indexed value sits at that path. 'rimsearcher code-search "[\w<>,\[\] ]+ Firefoam;"' finds the declaration, which does say.
--- stderr ---
