$ rimsearcher inherit Bullet_Revolver --exact-path projectile.damageAmountBase
exit 0
--- stdout ---
def_name        Bullet_Revolver
def_type        ThingDef
abstract        no
inherits_from   BaseBullet
mod             ludeon.rimworld
source          Projectiles_Guns.xml
patch_ops_name  n/a
2 ancestors within --exact-path projectile.damageAmountBase.

Same in every row, not repeated below: def_type=ThingDef, abstract=yes, mod=ludeon.rimworld, source=Projectiles_Guns.xml.
name            patch_ops_name
BaseBullet      2
BaseProjectile  0
A def inherits its ancestors' fields, so a patch that rewrites an ancestor changes what the game read for 'Bullet_Revolver' as well — and the patch_ops_name on 'Bullet_Revolver' itself does not count that. Above, patch_ops_name is not zero for 1 ancestor.
'Bullet_Revolver' carries 1 field matching 'projectile.damageAmountBase', all reading '12' — that is the value the counts below are compared against.

Same in every row, not repeated below: with_path=0, same_value=0, type_with_path=1, type_defs=9.
layer           other_defs
BaseBullet      0
BaseProjectile  2

layer                    state        next
patch_ops_defname_label  pre-measure  rimsearcher export --modlist fixture
--- stderr ---
