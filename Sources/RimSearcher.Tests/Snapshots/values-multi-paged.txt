$ rimsearcher values compClass thingClass --limit 1
exit 0
--- stdout ---
3 values of 'compClass', showing the first 1.

asked            compClass
matched_paths    comps[0].compClass (3), comps[1].compClass (2)
def_types        ThingDef (2 of 9), HediffDef (1 of 1)
defs_with_field  3
This table's axis is the field: it lists the values 'compClass' takes. Which fields hold a value you already have is the inverse question and a different command — 'rimsearcher where --value RimWorld.CompShield --exact' asks it for 'RimWorld.CompShield', the first row here. A value domain read here does not bound where those values occur.
4 values of 'thingClass', showing the first 1.

asked            thingClass
matched_paths    thingClass (6)
def_types        ThingDef (6 of 9)
defs_with_field  6
This table's axis is the field: it lists the values 'thingClass' takes. Which fields hold a value you already have is the inverse question and a different command — 'rimsearcher where --value RimWorld.Apparel --exact' asks it for 'RimWorld.Apparel', the first row here. A value domain read here does not bound where those values occur.

Defs that may be missing from this answer, their export cut short — every def type that uses any of these paths at all
def_type  defs_cut_short
ThingDef  1
verify: rimsearcher snapshot truncated --type ThingDef

Same in every row, not repeated below: defs=2.
value                field_path
RimWorld.CompShield  compClass
RimWorld.Apparel     thingClass

The values of 'compClass' come from 2 field paths pooled together, not from one field. Any path named for it above goes back in with --exact-path to pool that one alone; '[]' there stands for any index.
--- stderr ---
