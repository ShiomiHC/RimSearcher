$ rimsearcher get TwinGun --defaults --path-contains costList --fixture-presence-text
exit 0
--- stdout ---
Matching 'costList': 3 fields, out of 4 fields on the def.

def_name     TwinGun
def_type     ThingDef
label        twin gun
class        Verse.ThingDef
mod          ludeon.rimworld
source       Guns.xml

Same in every row, not repeated below: code_default=no.
path                  value  xml
costList[0].thingDef  Steel  here
costList[0].count     75     under costList
costList[0].quality   75     under costList
No value above with 'code_default'=no is one that most of the 5 ThingDefs in this snapshot also carry.
The game also prices things like this — market value, cost to make, work amount — and those are computed, not stored, so no field above holds them. This snapshot never measured them: 'rimsearcher economy TwinGun' says why and what to re-export.
--- stderr ---
