$ rimsearcher values compClass zzznotafield
exit 0
--- stdout ---
3 values of 'compClass'.

asked            compClass
matched_paths    comps[0].compClass (3), comps[1].compClass (2)
def_types        ThingDef (2 of 9), HediffDef (1 of 1)
defs_with_field  3
This table's axis is the field: it lists the values 'compClass' takes. Which fields hold a value you already have is the inverse question and a different command — 'rimsearcher where --value RimWorld.CompShield --exact' asks it for 'RimWorld.CompShield', the first row here.
  - No def in this snapshot has a field path ending in 'zzznotafield'. 'rimsearcher fields <DefType>' lists the paths a type actually has, and 'rimsearcher where --value <text>' finds which path holds a value you already know.
  - No indexed value sits at that path. 'rimsearcher code-search "[\w<>,\[\] ]+ zzznotafield;"' finds the declaration, which does say. The exporter stopped short on some defs — past their field cap, past the depth cap, or partway down a list — and a path cut that way is not indexed either: 'rimsearcher snapshot truncated' lists those defs. This snapshot (exporter 0.2.0) does not list the fields a type can have, so it cannot tell a field that is null on every def from one the type does not have. The runtime type of a nested Class="..." object is indexed as '<path>.Class' for list elements only (<li Class="...">), so 'rimsearcher where Class <ClassName>' reaches those. A single field that picks a class — GenStepDef.genStep, ThinkTreeDef.thinkRoot — is not in this snapshot at all: it was written by exporter 0.2.0, before that case entered the index. Re-export to reach it.

Defs that may be missing from this answer, their export cut short — every def type that uses this path at all
def_type  defs_cut_short
ThingDef  1
verify: rimsearcher snapshot truncated --type ThingDef

Same in every row, not repeated below: field_path=compClass.
value                        defs
RimWorld.CompShield          2
TestMod.CompBoltedOn         2
Verse.HediffComp_Disappears  1

The values of 'compClass' come from 2 field paths pooled together, not from one field. Any path named for it above goes back in with --exact-path to pool that one alone; '[]' there stands for any index.
--- stderr ---
