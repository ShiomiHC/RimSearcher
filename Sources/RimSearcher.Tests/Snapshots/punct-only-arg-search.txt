$ rimsearcher search .
exit 0
--- stdout ---
'.' holds no letter or digit, so the rows below come from scanning text for it as a plain substring.
  - These defs have it in the original text a translation replaced.
  - 2 defs.

Same in every row, not repeated below: declared_in=ludeon.rimworld.
def_name  def_type  label     matched_on
Firefoam  ThingDef  firefoam  description
Firefoam  StatDef             same def_name
--- stderr ---
