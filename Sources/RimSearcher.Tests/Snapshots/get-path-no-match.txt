$ rimsearcher get Apparel_ShieldBelt --path-contains zzzz
exit 0
--- stdout ---
No field path contains 'zzzz'; the def does have 13 fields. Drop --path-contains to see them.
No indexed value sits at that path. 'rimsearcher code-search "[\w<>,\[\] ]+ zzzz;"' finds the declaration, which does say. The exporter stopped short on some defs — past their field cap, past the depth cap, or partway down a list — and a path cut that way is not indexed either: 'rimsearcher snapshot truncated' lists those defs. This snapshot (exporter 0.2.0) does not list the fields a type can have, so it cannot tell a field that is null on every def from one the type does not have. The runtime type of a nested Class="..." object is indexed as '<path>.Class' for list elements only (<li Class="...">), so 'rimsearcher where Class <ClassName>' reaches those. A single field that picks a class — GenStepDef.genStep, ThinkTreeDef.thinkRoot — is not in this snapshot at all: it was written by exporter 0.2.0, before that case entered the index. Re-export to reach it.

def_name     Apparel_ShieldBelt
def_type     ThingDef
label        shield belt
class        Verse.ThingDef
declared_in  ludeon.rimworld
source       Apparel_Belts.xml
xml          not indexed (exporter 0.2.0)
Market value, cost to make and work amount are not fields: 'rimsearcher economy Apparel_ShieldBelt' has them.

0 rows. Dropping any one option below brings back this many translations:
filter                hidden  next
--path-contains zzzz  1       rimsearcher get Apparel_ShieldBelt

layer           state        next
injection_keys  pre-measure  rimsearcher export --modlist fixture
--- stderr ---
