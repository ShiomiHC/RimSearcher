$ rimsearcher search CompProperties_NoSuchThing
exit 1
--- stdout ---
Nothing matched 'CompProperties_NoSuchThing' in this snapshot.
Nothing in this snapshot is called that under any other guise either — no def type, no class, no mod. The runtime type of a nested Class="..." object is indexed as '<path>.Class' for list elements only (<li Class="...">), so 'rimsearcher where Class <ClassName>' reaches those. A single field that picks a class — GenStepDef.genStep, ThinkTreeDef.thinkRoot — is not in this snapshot at all: it was written by exporter 0.2.0, before that case entered the index. Re-export to reach it. 'rimsearcher code-search "class CompProperties_NoSuchThing\b"' finds the class declaration; 'rimsearcher code-search "CompProperties_NoSuchThing"' lists who uses it.
--- stderr ---
