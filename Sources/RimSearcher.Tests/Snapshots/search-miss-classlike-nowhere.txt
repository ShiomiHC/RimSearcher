$ rimsearcher search CompProperties_NoSuchThing
exit 1
--- stdout ---
Nothing matched 'CompProperties_NoSuchThing' in this snapshot.
Nothing in this snapshot is called that under any other guise either — no def type, no class, no mod. The runtime type of a nested Class="..." object — in a list or on a single field — is indexed as '<path>.Class', so 'rimsearcher where Class <ClassName>' is the query that reaches it. 'rimsearcher code-search "class CompProperties_NoSuchThing\b"' finds the class declaration; 'rimsearcher code-search "CompProperties_NoSuchThing"' lists who uses it.
--- stderr ---
