$ rimsearcher read Outline.cs --source vanilla --outline --limit 2
exit 0
--- stdout ---
9 declarations in vanilla/Verse/Outline.cs, showing the first 2; raise --limit to see the rest.

Same in every row, not repeated below: in=Outer, lines=1.
kind   modifiers                name    at
field  private static readonly  Marker  9-9
field  private                  Open    11-11

Found by matching braces, not by parsing C#: a declaration this scan does not recognise and one that is not in the file look the same here — both are simply absent. Operators are one kind it does not recognise.
--- stderr ---
