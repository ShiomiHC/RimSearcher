$ rimsearcher where CompShield
exit 4
--- stdout ---
No def in this snapshot has a field path ending in 'CompShield'. 'rimsearcher fields <DefType> --path-contains <text>' lists the paths that a def type actually has.

Where the name does turn up:
name        is           in                                        next
CompShield  field value  comps[0].compClass = RimWorld.CompShield  rimsearcher where compClass CompShield
No indexed value sits at that path. 'rimsearcher code-search "[\w<>,\[\] ]+ CompShield;"' finds the declaration, which does say.
--- stderr ---
