$ rimsearcher get ChildGun --defaults --fixture-presence-text
exit 0
--- stdout ---
13 fields.

def_name       ChildGun
def_type       ThingDef
label          child gun
class          Verse.ThingDef
mod            ludeon.rimworld
source         Guns.xml
inherits_from  BaseGun (see 'rimsearcher inherit ChildGun')

path                          value            code_default  xml
thingClass                    RimWorld.Bullet  no            parent
damage                        12               no            here
speed                         70               no            parent
costList[0].thingDef          Steel            no            here
costList[0].count             75               no            here
costList[0].quality           Normal           yes           not-written
statBases[0].stat             Beauty           no            here
statBases[0].value            -4               no            here
things[0].def                 Widget           no            here
things[0].chance              0.25             no            here
things[0].hp                  0                yes           not-written
descriptionHyperlinks[0].def  ChildGun         no            under descriptionHyperlinks
burstCount                    1                yes           not-written
No value above with 'code_default'=no is one that most of the 5 ThingDefs in this snapshot also carry. Rows marked yes were not compared: what a yes leaves open is settled by the 'xml' column on that same row: here is an XML line writing that same value, not-written is the absence of such a line in the XML read before patches ran.
  - 3 patch xpaths name this def. The 'xml' column above reads the XML as written on disk, before any PatchOperation ran, so a line one of those patches added reads not-written there — 'rimsearcher inherit ChildGun' breaks the count down by how each xpath names it.
  - The game also prices things like this — market value, cost to make, work amount — and those are computed, not stored, so no field above holds them. This snapshot never measured them: 'rimsearcher economy ChildGun' says why and what to re-export.
--- stderr ---
