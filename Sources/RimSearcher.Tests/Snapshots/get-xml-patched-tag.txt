$ rimsearcher get PatchListGun --defaults --fixture-presence-patch
exit 0
--- stdout ---
3 fields.

def_name     PatchListGun
def_type     ThingDef
label        patch list gun
class        Verse.ThingDef
mod          ludeon.rimworld
source       Guns.xml

path                  value   code_default  xml
costList[0].thingDef  Steel   no            here+patch
costList[0].count     75      no            here+patch
costList[0].quality   Normal  yes           no
No value above with 'code_default'=no is one that most of the 2 ThingDefs in this snapshot also carry. Rows marked yes were not compared: a yes is not evidence that nothing wrote the value — the 'xml' column on that same row tells the two apart: here is an XML line writing that same value, no is an XML that does not write it (read after every patch ran).
The game also prices things like this — market value, cost to make, work amount — and those are computed rather than stored, so no field above would hold them either way. This snapshot never measured them, so it cannot say whether this def is one of the priced ones: 'rimsearcher economy PatchListGun' says why and what to re-export.
--- stderr ---
