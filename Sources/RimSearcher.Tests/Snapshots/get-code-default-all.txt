$ rimsearcher get Bullet_Revolver --defaults
exit 0
--- stdout ---
8 fields.
The exporter stopped short on this def: at least 3 fields were dropped at export time for depth or size, so a path missing from the list below is not proof that the def lacks it.

def_name       Bullet_Revolver
def_type       ThingDef
label          revolver bullet
class          Verse.ThingDef
mod            ludeon.rimworld
source         Projectiles_Guns.xml
inherits_from  BaseBullet (see 'rimsearcher inherit Bullet_Revolver')
A `yes` in code_default means the value is identical to what a freshly constructed instance carries, and this snapshot cannot tell those two apart: it supports neither 'the def sets this' nor 'the def leaves it to the class default'. Reading the C# constructor shows where a default could come from, never whether the XML repeats it. Only `no` decides: something set that value. `unknown` is not a third answer: the type could not be constructed, so nothing was compared.

path                         value                code_default
soundImpactDefault           BulletImpact_Ground  no
soundDrop                    Standard_Drop        no
soundPickup                  Standard_Pickup      no
soundInteract                Standard_Pickup      no
thingClass                   RimWorld.Bullet      no
projectile.damageAmountBase  12                   no
projectile.burstCount        1                    yes
projectile.speed             70                   unknown
Values that most of the 9 ThingDefs in this snapshot also carry, so their 'code_default' is not this def having made a choice — the count in brackets: soundDrop (9), soundImpactDefault (9), soundInteract (9), soundPickup (9). Only rows whose 'code_default' is no were compared: a yes is a declared default already.
--- stderr ---
