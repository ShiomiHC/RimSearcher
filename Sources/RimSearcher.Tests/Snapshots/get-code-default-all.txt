$ rimsearcher get Bullet_Revolver --defaults
exit 0
--- stdout ---
8 fields.
The exporter stopped short on this def: at least 3 fields were dropped at export time for depth or size, so a path missing from the list below is not proof that the def lacks it. Added to the 8 paths that did get indexed, that is at least 11 field paths on this def.

def_name       Bullet_Revolver
def_type       ThingDef
label          revolver bullet
class          Verse.ThingDef
mod            ludeon.rimworld
source         Projectiles_Guns.xml
inherits_from  BaseBullet (see 'rimsearcher inherit Bullet_Revolver')

path                         value                code_default
soundImpactDefault           BulletImpact_Ground  no
soundDrop                    Standard_Drop        no
soundPickup                  Standard_Pickup      no
soundInteract                Standard_Pickup      no
thingClass                   RimWorld.Bullet      no
projectile.damageAmountBase  12                   no
projectile.burstCount        1                    yes
projectile.speed             70                   unknown
Values that most of the 9 ThingDefs in this snapshot also carry, so their 'code_default' is not this def having made a choice — the count in brackets: soundDrop (9), soundImpactDefault (9), soundInteract (9), soundPickup (9). Only rows whose 'code_default' is no were compared: a yes is not evidence that nothing wrote the value — it only says the value matches what a fresh instance of the declaring type carries.
--- stderr ---
