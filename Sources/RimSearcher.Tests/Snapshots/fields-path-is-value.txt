$ rimsearcher fields ThingDef --path-contains MarketValue
exit 1
--- stdout ---
'ThingDef' has field paths, but none contains 'MarketValue'. Drop --path-contains to see them all.
'MarketValue' is a value on 'ThingDef', not a field name: it sits at statBases[].stat / statFactors[].stat on 3 defs. 'rimsearcher where --value MarketValue --type ThingDef' asks it.
--- stderr ---
