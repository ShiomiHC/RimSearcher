$ rimsearcher code-search zzzznothing
exit 1
--- stdout ---
No line matched in 9 files under '*.cs' across 2 of 3 source trees on disk — the rest hold no file matching --file-glob '*.cs', and 'sources list' says which have never been decompiled. These are decompiler output. Local variable names do not survive it — ILSpy re-invents them from the assignment, giving names like num, num2, list and flag, so 'zzzznothing' can only turn up here if it is a type, member, parameter or string literal name, never if it was a local. If you were looking for a def rather than code, 'rimsearcher search' and 'rimsearcher where' answer that from the snapshot — the XML is not searched here.
--- stderr ---
