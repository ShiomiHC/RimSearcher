$ rimsearcher keyed 转至此处
exit 0
--- stdout ---
2 keys.

Same in every row, not repeated below: translated=转至此处, origin=in effect, placeholder=no.
key             original          mod  source
ClickToJump     Click to jump          Alerts.xml:5
JumpToLocation  Jump to location       Letters.xml:6
These rows do not all carry the same key, so the code search goes after whichever row is the one you meant: 'rimsearcher code-search "\"<key>\""' with the key from that row. Most call sites write the key inline, but not all of them do — a key assembled from parts will not appear that way.

This snapshot never scanned the language files on disk, so every row here is one the game actually had: the absence of an 'on disk' row is not evidence that no installed mod translates it. No 'mod_roots' is configured on this machine, so there is nowhere to scan: set it in the config file and import again to measure that layer.
--- stderr ---
