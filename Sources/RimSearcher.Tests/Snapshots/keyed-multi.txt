$ rimsearcher keyed CannotUseNoPower 没有电力
exit 0
--- stdout ---
1 keyed translation for 'CannotUseNoPower'.
  - 1 key for '没有电力'.
  - To find the code that prints it, search for the key as a literal: 'rimsearcher code-search "\"CannotUseNoPower\""'. A key assembled from parts is not written inline anywhere, so that search does not reach it.

Same in every row, not repeated below: translated=没有电力, original=No power, origin=in effect, placeholder=no, source=Misc.xml:12.
key               mod  query
CannotUseNoPower       CannotUseNoPower
CannotUseNoPower       没有电力

This snapshot never scanned the language files on disk, so every row here is one the game actually had: the absence of an 'on disk' row is not evidence that no installed mod translates it. No 'mod_roots' is configured on this machine, so there is nowhere to scan: set it in the config file and import again to measure that layer.
--- stderr ---
