$ rimsearcher inherit BaseProjectile
exit 0
--- stdout ---
name           BaseProjectile
def_type       ThingDef
abstract       yes
mod            ludeon.rimworld
source         Projectiles_Guns.xml
patch_ops      0
No patch operation's xpath names 'BaseProjectile' with @Name= in this snapshot — that is what the 0 counts. An xpath that reaches it any other way leaves no trace here, so the 0 is not evidence that the game read this node unpatched.
3 direct children.

name        def_name    def_type        abstract  mod
BaseBullet              ThingDef        yes       ludeon.rimworld
            Firefoam    ThingDef        no        ludeon.rimworld
            VariantOne  TestVariantDef  no        test.mod
An abstract node has no fields of its own in a snapshot. Everything it declares is already merged, post-patch, into each child: 'rimsearcher get Firefoam'.
--- stderr ---
