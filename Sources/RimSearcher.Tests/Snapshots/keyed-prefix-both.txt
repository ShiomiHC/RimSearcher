$ rimsearcher keyed CommandSettl
exit 0
--- stdout ---
2 keys for 'CommandSettl'.
These rows do not all carry the same key, so the code search goes after whichever row is the one you meant: 'rimsearcher code-search "\"<key>\""' with the key from that row. A key assembled from parts is not written inline anywhere, so that search does not reach it.

Same in every row, not repeated below: origin=in effect, placeholder=no, query=CommandSettl.
key                translated            original                  mod  source
CommandSettle      定居                  Settle                         GameplayCommands.xml:386
CommandSettleDesc  在此处建立新殖民地。  Settle in this location.       GameplayCommands.xml:387

This snapshot never scanned the language files on disk, so every row here is one the game actually had: the absence of an 'on disk' row is not evidence that no installed mod translates it. No 'mod_roots' is configured on this machine, so there is nowhere to scan: set it in the config file and import again to measure that layer.
--- stderr ---
