$ rimsearcher where HediffDef Anesthetic
exit 1
--- stdout ---
'HediffDef' is also a def type here (1 def), and this query reads it as a field path, not as a type. For the type: 'rimsearcher where <fieldPath> Anesthetic --type HediffDef'.
  - No def in this snapshot has a field path ending in 'HediffDef'. 'rimsearcher fields <DefType> --path-contains <text>' lists the paths that a def type actually has, and 'rimsearcher where --value Anesthetic' finds which field holds that value.
  - No indexed value sits at that path. 'rimsearcher code-search "[\w<>,\[\] ]+ HediffDef;"' finds the declaration, which does say.
--- stderr ---
