$ rimsearcher where graphicData.texPath
exit 0
--- stdout ---
2 defs.

Defs that may be missing from this answer, their export cut short — every def type that uses this path at all
def_type  defs_cut_short
ThingDef  1
verify: rimsearcher snapshot truncated --type ThingDef

Same in every row, not repeated below: def_type=ThingDef, path=graphicData.texPath, code_default=no, mod=ludeon.rimworld.
def_name    value
Firefoam    Things/Building/Nozzle
FoamPopper  Things/Building/Cannon
--- stderr ---
