$ rimsearcher get Firefoam
exit 0
--- stdout ---
2 defs share the name 'Firefoam' across different def types; all of them are shown. Pass --type <DefType> for just one.
What follows is 2 defs in a row, one block each. Every count, footnote and truncation warning below belongs to the block it sits in — read the def_type line at the top of a block to know which def the lines under it are about.

def_name       Firefoam
def_type       ThingDef
label          firefoam
class          Verse.ThingDef
mod            ludeon.rimworld
source         Buildings_Special.xml
inherits_from  BaseProjectile (see 'rimsearcher inherit Firefoam')
8 fields.

Same in every row, not repeated below: code_default=no.
path                                   value
soundImpactDefault                     BulletImpact_Ground
soundDrop                              Standard_Drop
soundPickup                            Standard_Pickup
soundInteract                          Standard_Pickup
thingClass                             RimWorld.Building
statBases[0].stat                      MarketValue
graphicData.texPath                    Things/Building/Nozzle
building.blueprintGraphicData.texPath  Things/Building/Blueprint_Nozzle
Values that most of the 9 ThingDefs in this snapshot also carry, so their 'code_default' is not this def having made a choice — the count in brackets: soundDrop (9), soundImpactDefault (9), soundInteract (9), soundPickup (9). Only rows whose 'code_default' is no were compared: a yes is a declared default already.
2 translations.

Same in every row, not repeated below: language=ChineseSimplified, origin=in effect.
path         translated      original
description  一团灭火泡沫。  A blob of firefoam.
label        灭火泡沫        firefoam

def_name     Firefoam
def_type     StatDef
class        Verse.StatDef
mod          ludeon.rimworld
source       Stats_Basics.xml
0 fields.
--- stderr ---
