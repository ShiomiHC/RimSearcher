$ rimsearcher where --value CompShield
exit 0
--- stdout ---
1 field path.
  - 2 defs hold it altogether, counting each def once.
  - No value here is exactly 'CompShield'; each match has it inside a longer value — see example_value. --exact would return nothing.

Defs that may be missing from this answer, their export cut short — every def type that holds this value anywhere
def_type  defs_cut_short
ThingDef  1
verify: rimsearcher snapshot truncated --type ThingDef

path                def_type  defs_exact  defs_other  example_value
comps[0].compClass  ThingDef  0           2           RimWorld.CompShield
--- stderr ---
