$ rimsearcher get PatchGun --defaults --fixture-presence-patch
exit 0
--- stdout ---
7 fields.

def_name       PatchGun
def_type       ThingDef
label          patch gun
class          Verse.ThingDef
mod            ludeon.rimworld
source         Guns.xml
inherits_from  BasePatchGun (see 'rimsearcher inherit PatchGun')

path                              value                 code_default  xml
comps[0].compClass                TestMod.CompBoltedOn  no            not-written
damage                            12                    no            here
speed                             70                    no            parent+patch
recipeMaker.researchPrerequisite  SpecializedLimbs      no            here+patch
costList[0].thingDef              Steel                 no            here
costList[0].count                 75                    no            here
costList[0].quality               Normal                yes           not-written
No value above with 'code_default'=no is one that most of the 2 ThingDefs in this snapshot also carry. Rows marked yes were not compared: what a yes leaves open is settled by the 'xml' column on that same row: here is an XML line writing that same value, not-written is the absence of such a line in the XML read after every patch ran.
The game also prices things like this — market value, cost to make, work amount — and those are computed rather than stored, so no field above would hold them either way. This snapshot never measured them, so it cannot say whether this def is one of the priced ones: 'rimsearcher economy PatchGun' says why and what to re-export.
--- stderr ---
