$ rimsearcher keyed filler --empty-translation
exit 0
--- stdout ---
1 key for 'filler' within --empty-translation.
To find the code that prints it, search for the key as a literal: 'rimsearcher code-search "\"FillerKey2099\""'. A key assembled from parts is not written inline anywhere, so that search does not reach it.

key            translated  original     origin     placeholder  mod  source           query
FillerKey2099  填充2099    filler line  in effect  yes               Filler.xml:2100  filler
Placeholder means the language file declares the key without a translation, so the game displays the English text instead of what the translated column shows.

This snapshot never scanned the language files on disk, so every row here is one the game actually had and no 'on disk' row could appear. No 'mod_roots' is configured on this machine, so there is nowhere to scan: set it in the config file and import again to measure that layer.
--- stderr ---
