$ rimsearcher search TestModGun --scope ludeon.rimworld
exit 1
--- stdout ---
Nothing matched 'TestModGun' in this snapshot within --scope ludeon.rimworld. This command covers def names, labels, descriptions and translations, not C# class names.
'TestModGun' is in this snapshot after all — it comes from test.mod, which --scope ludeon.rimworld excludes. Drop --scope, or name that mod in it.
--- stderr ---
