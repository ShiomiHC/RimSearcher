$ rimsearcher where statBases.stat --exact-path
exit 1
--- stdout ---
No def in this snapshot has a field path ending in 'statBases.stat'. 'rimsearcher fields <DefType> --path-contains <text>' lists the paths that a def type actually has, and 'rimsearcher where --value <text>' finds which path holds a value you already know.
No indexed value sits at that path. 'rimsearcher code-search "[\w<>,\[\] ]+ stat;"' finds the declaration, which does say.
--- stderr ---
