$ rimsearcher get ChildGun --defaults --path-contains costList --path-contains things --path-contains Hyperlinks --fixture-presence
exit 0
--- stdout ---
Matching 'costList' or 'things' or 'Hyperlinks': 7 fields, out of 14 fields on the def. Whole path segment: 6 fields; inside a longer name: 1 field.

def_name       ChildGun
def_type       ThingDef
label          child gun
class          Verse.ThingDef
mod            ludeon.rimworld
source         Guns.xml
inherits_from  BaseGun (see 'rimsearcher inherit ChildGun')

path                          value     code_default  xml
costList[0].thingDef          Steel     no            here
costList[0].count             75        no            under costList
costList[0].quality           Normal    yes           under costList
things[0].def                 Widget    no            here
things[0].chance              0.25      no            here
things[0].hp                  0         yes           no
descriptionHyperlinks[0].def  ChildGun  no            under descriptionHyperlinks
No value above with 'code_default'=no is one that most of the 2 ThingDefs in this snapshot also carry. Rows marked yes were not compared: what a yes leaves open is settled by the 'xml' column on that same row: here is an XML line writing that same value, no is an XML that does not write it (read before patches ran).
  - 3 patch xpaths name this def. The 'xml' column above reads the XML as written on disk, before any PatchOperation ran, so a line one of those patches added reads no there — 'rimsearcher inherit ChildGun' breaks the count down by how each xpath names it.
  - The game also prices things like this — market value, cost to make, work amount — and those are computed rather than stored, so no field above would hold them either way. This snapshot never measured them, so it cannot say whether this def is one of the priced ones: 'rimsearcher economy ChildGun' says why and what to re-export.
--- stderr ---
