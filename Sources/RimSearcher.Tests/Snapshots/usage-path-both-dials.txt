$ rimsearcher get Apparel_ShieldBelt --path-contains comps --exact-path comps[0].compClass
exit 2
--- stdout ---
--- stderr ---
--path-contains and --exact-path cannot be combined: one matches the path as a substring, the other as a whole. Pass --path-contains <text> for a fragment, or --exact-path <path> for a complete field path.
