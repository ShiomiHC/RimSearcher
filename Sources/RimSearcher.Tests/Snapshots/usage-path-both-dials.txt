$ rimsearcher get Apparel_ShieldBelt --path-contains comps --exact-path comps[0].compClass
exit 2
--- stdout ---
--- stderr ---
--path-contains and --exact-path cannot be combined: one matches a fragment of the path, the other matches it end to end. Pass --path-contains <text> for a fragment, or --exact-path <path> for a complete field path.
