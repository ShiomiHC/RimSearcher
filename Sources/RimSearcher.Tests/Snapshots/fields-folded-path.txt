$ rimsearcher fields ThingDef --path-contains comps[].props
exit 0
--- stdout ---
2 field paths on ThingDef within --path-contains comps[].props.
Matching 'comps[].props': 2 field paths, out of 26 field paths on ThingDef.

Defs that may be missing from this answer, their export cut short — all of ThingDef, whatever --path-contains says
def_type  defs_cut_short
ThingDef  1
verify: rimsearcher snapshot truncated --type ThingDef

Same in every row, not repeated below: defs=1, def_type=ThingDef.
path
comps[0].props.energyLossPerDamage
comps[0].props.energyMax
--- stderr ---
