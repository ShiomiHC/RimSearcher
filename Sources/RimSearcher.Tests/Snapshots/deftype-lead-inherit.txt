$ rimsearcher inherit ThingDef Bullet_Revolver
exit 0
--- stdout ---
No XML node answers to ThingDef, so nothing below is about it. The blocks that did come back are unaffected.

Where the name does turn up:
name      is        in      next
ThingDef  def type  9 defs  rimsearcher list ThingDef

def_name        Bullet_Revolver
def_type        ThingDef
abstract        no
inherits_from   BaseBullet
declared_in     ludeon.rimworld
source          Projectiles_Guns.xml
patch_ops_name  n/a
2 ancestors.

Same in every row, not repeated below: def_type=ThingDef, abstract=yes, declared_in=ludeon.rimworld, source=Projectiles_Guns.xml.
name            patch_ops_name
BaseBullet      2
BaseProjectile  0
A def inherits its ancestors' fields, so a patch that rewrites an ancestor changes what the game read for 'Bullet_Revolver' as well — and the patch_ops_name on 'Bullet_Revolver' itself does not count that. Above, patch_ops_name is not zero for 1 ancestor.

layer                    state        next
patch_ops_defname_label  pre-measure  rimsearcher export --modlist fixture
--- stderr ---
