$ rimsearcher search CompShield
exit 1
--- stdout ---
Nothing matched 'CompShield' in this snapshot.
'CompShield' is not a def name, but it appears as a field value: 'comps[0].compClass' holds 'RimWorld.CompShield'. 'rimsearcher where compClass CompShield' lists the defs that use it, and 'rimsearcher where --value CompShield' asks the same without naming a path.
--- stderr ---
