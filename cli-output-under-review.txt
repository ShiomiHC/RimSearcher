$ rimsearcher types Verse.ThingComp --derived --transitive --declares CompTick --limit 5
Read from where the game has it installed rather than from a copy kept with the source tree — 1 assembly: shiomi.universalliftstructure/Universal_Lift_Structure. What is below and what 'rimsearcher read' shows can therefore be two different builds. 'rimsearcher sources sync' keeps a copy alongside the C#, after which both come from one dll.
  - Of 1075 derived types, the ones declaring 'CompTick' themselves. The rest inherit it rather than lacking it.
  - 375 derived types, showing the first 5; --limit all shows every one

Same in every row, not repeated below: namespace=AncotLibrary, base=Verse.ThingComp, compiler_generated=no.
assembly      type                                  interfaces                      derived
AncotLibrary  AncotLibrary.CompAerocraft                                            0
AncotLibrary  AncotLibrary.CompThingCarrier_Custom  Verse.IThingHolder              1
AncotLibrary  AncotLibrary.CompApplyHediffArea                                      0
AncotLibrary  AncotLibrary.CompTurretGun_Building   Verse.AI.IAttackTargetSearcher  0
AncotLibrary  AncotLibrary.CompSustainedShoot                                       0

$ rimsearcher types Verse.Pawn --bases
Read from where the game has it installed rather than from a copy kept with the source tree — 1 assembly: shiomi.universalliftstructure/Universal_Lift_Structure. What is below and what 'rimsearcher read' shows can therefore be two different builds. 'rimsearcher sources sync' keeps a copy alongside the C#, after which both come from one dll.
  - The chain from Verse.Pawn ends at System.Object, which is not in any tree read here — that is where it leaves the synced assemblies, not where it stops having a base.
  - 4 types.

assembly         type                  namespace  base           interfaces                                                                derived  compiler_generated
Assembly-CSharp  Verse.ThingWithComps  Verse      Verse.Thing                                                                              58       no
Assembly-CSharp  Verse.Thing           Verse      Verse.Entity   Verse.ISelectable, Verse.ILoadReferenceable, RimWorld.ISignalReceiver, …  37       no
Assembly-CSharp  Verse.Entity          Verse      System.Object                                                                            1        no
                 System.Object

$ rimsearcher types NoSuchTypeAtAll
Read from where the game has it installed rather than from a copy kept with the source tree — 1 assembly: shiomi.universalliftstructure/Universal_Lift_Structure. What is below and what 'rimsearcher read' shows can therefore be two different builds. 'rimsearcher sources sync' keeps a copy alongside the C#, after which both come from one dll.
No type named 'NoSuchTypeAtAll' is in the 78 assemblies read here. A type only shows up here if the mod that defines it is in the snapshot and its tree has been synced — 'rimsearcher sources list' says which trees those are.

$ rimsearcher members Verse.ThingComp --inherited --limit 6
Read from where the game has it installed rather than from a copy kept with the source tree — 1 assembly: shiomi.universalliftstructure/Universal_Lift_Structure. What is below and what 'rimsearcher read' shows can therefore be two different builds. 'rimsearcher sources sync' keeps a copy alongside the C#, after which both come from one dll.
85 members, showing the first 6; --limit all lists every one

Same in every row, not repeated below: type=Verse.ThingComp, kind=method, static=no, virtual=yes, abstract=no, override=no, accessibility=public.
assembly         member             signature
Assembly-CSharp  get_ParentHolder   IThingHolder get_ParentHolder()
Assembly-CSharp  Initialize         void Initialize(CompProperties)
Assembly-CSharp  ReceiveCompSignal  void ReceiveCompSignal(string)
Assembly-CSharp  PostExposeData     void PostExposeData()
Assembly-CSharp  PostSpawnSetup     void PostSpawnSetup(bool)
Assembly-CSharp  PostMapInit        void PostMapInit()

$ rimsearcher callers Verse.Pawn.Kill --source vanilla
Read from where the game has it installed rather than from a copy kept with the source tree — 1 assembly: shiomi.universalliftstructure/Universal_Lift_Structure. What is below and what 'rimsearcher read' shows can therefore be two different builds. 'rimsearcher sources sync' keeps a copy alongside the C#, after which both come from one dll.
Nothing in the 1 source tree searched calls Verse.Pawn::Kill. It overrides Kill, which the base chain declares as well — Verse.ThingWithComps, Verse.Thing. A call written against one of those types names that type, so its call sites are counted under it, not here: 'rimsearcher callers Verse.Thing.Kill' asks the outermost one.

$ rimsearcher callers Verse.Thing.Kill --source vanilla --limit 3
Read from where the game has it installed rather than from a copy kept with the source tree — 1 assembly: shiomi.universalliftstructure/Universal_Lift_Structure. What is below and what 'rimsearcher read' shows can therefore be two different builds. 'rimsearcher sources sync' keeps a copy alongside the C#, after which both come from one dll.
  - 59 callers, showing the first 3; --limit all lists every one
  - Reflection and Harmony patches call by name at run time, and those calls are in no instruction stream — they are absent from the count above rather than reported as zero. 'rimsearcher code-search' finds the strings that name a method.

Same in every row, not repeated below: from_assembly=Assembly-CSharp, to_assembly=Assembly-CSharp, to_type=Verse.Thing, to_member=Kill.
tree     from_type                                from_member          call_sites
vanilla  Verse.Pawn_HealthTracker                 CheckForStateChange  3
vanilla  MechanitorUtility+<>c__DisplayClass21_0  <GetMechGizmos>b__3  1
vanilla  Verse.DamageWorker                       Apply                1

$ rimsearcher callers Verse.Pawn.Tick --callees --limit 3
Read from where the game has it installed rather than from a copy kept with the source tree — 1 assembly: shiomi.universalliftstructure/Universal_Lift_Structure. What is below and what 'rimsearcher read' shows can therefore be two different builds. 'rimsearcher sources sync' keeps a copy alongside the C#, after which both come from one dll.
  - Searched without a call-graph table, and therefore not searched at all — 1 source tree: shiomi.universalliftstructure. If the method named above lives in one of them, everything it calls is missing from below. 'rimsearcher sources sync' builds the tables.
  - 40 callees, showing the first 3; --limit all lists every one

Same in every row, not repeated below: from_assembly=Assembly-CSharp, from_type=Verse.Pawn, from_member=Tick, to_assembly=Assembly-CSharp.
tree     to_type                            to_member      call_sites
vanilla  Verse.Thing                        get_Spawned    8
vanilla  Verse.Pawn                         get_RaceProps  4
vanilla  RimWorld.Planet.WorldPawnsUtility  IsWorldPawn    2

$ rimsearcher types --help
Usage: rimsearcher types <name> [options]

Find C# types and show what they derive from and what derives from them.

The name can be a full one ('Verse.ThingComp'), a bare one ('ThingComp'), or a fragment — a full name is tried first, then a bare one, then anything ending in it, and the first of those that matches is what you get. Several types can carry the same bare name across mods; all of them are listed rather than one being picked.

--derived walks downward and --bases upward. Both stop at the edge of the synced trees: System.Object and the rest of the framework are not in any tree, so a base chain ending somewhere else is the chain leaving what was read, not a gap in it.

Arguments:
  <name>  A type name: 'Verse.ThingComp', 'ThingComp', or a fragment of one.

Options:
      --source <tree>       Which decompiled source tree to read. Omit to read them all.
                            Also accepted: --tree, --from-tree.
      --derived             List the types that derive from it, or implement it when it is an interface. Direct ones only unless --transitive.
                            Also accepted: --subclasses, --implementors, --children.
      --transitive          With --derived, follow the chain all the way down instead of one level.
                            Also accepted: --deep, --recursive.
      --bases               List the chain of base types upward instead.
                            Also accepted: --base-types, --parents, --ancestors.
      --declares <member>   Only types that declare a member by this name. With --derived this is the set of types that override it — a derived type that declares nothing by that name inherits the base implementation.
                            Also accepted: --declaring, --with-member.
      --namespace <prefix>  Only types whose namespace starts with this.
                            Also accepted: --ns, --in-namespace.
  -n, --limit <n>           How many types to return, at most. Left out, every one is returned. Default: every one.
                            Also accepted: --max-results, --count, --top, --rows, --num, --head.

Global options (every command takes these, written after the command):
      --snapshot <name>  Query this named snapshot instead of the one that would be picked automatically. An explicit choice always wins over auto-detection.
                         Also accepted: --snap, --env.
      --db <path>        Query the snapshot database at this path directly, bypassing the registry.
                         Also accepted: --database, --snapshot-path.
      --json             Emit machine-readable JSON. Anything the text output would have said in prose moves into a 'notes' array, so nothing is lost. The command's own table key is always present — an empty array when nothing matched, never a missing key. A note that reports a count also carries 'shown' and 'total' as numbers, so the figures never have to be parsed back out of its text; 'total' is null when only a lower bound is known, and both keys are absent on notes that are not counts.
      --config <path>    Use this config file instead of the default one.

--json keys (besides 'notes'):
  types  one row per type: assembly, type, namespace, base, interfaces, derived, compiler_generated.

Examples:
  rimsearcher types Verse.ThingComp --derived --transitive
  rimsearcher types CompProperties --namespace RimWorld
  rimsearcher types Verse.Pawn --bases
  rimsearcher types Verse.ThingComp --derived --transitive --declares CompTick

$ rimsearcher members --help
Usage: rimsearcher members <type> [options]

List the members of a C# type, filtered by kind and by the modifiers on them.

Members are read from the assembly, not from the decompiled C#, so the filters below are the metadata bits themselves rather than a guess at keywords in the text.

Only what the type declares is listed. Inherited members live on the base type and are not repeated here — --inherited walks the chain and lists those too, each under the type that declares it.

A property appears twice over: once as itself under the C# name, and once as the get_/set_ methods IL actually holds. 'rimsearcher il' takes the latter.

Arguments:
  <type>  A type name: 'Verse.ThingComp', 'ThingComp', or a fragment of one.

Options:
      --source <tree>       Which decompiled source tree to read. Omit to read them all.
                            Also accepted: --tree, --from-tree.
      --name <text>         Only members whose name contains this text.
                            Also accepted: --contains, --member-name.
      --member-kind <kind>  Only one kind: method, constructor, property, field, or event. Comma-separated for several.
                            Also accepted: --kinds, --of-kind.
      --static              Only static members.
                            Also accepted: --statics.
      --instance            Only instance members.
                            Also accepted: --non-static.
      --virtual             Only virtual members — the ones a subclass can override.
                            Also accepted: --virtuals, --patchable.
      --abstract            Only abstract members. These have no body of their own; the implementations are on the types that derive from this one.
                            Also accepted: --abstracts.
      --overrides           Only members that override a base one. The test is the metadata bit, not a name match — a member that merely shares a name with a base member is not an override.
                            Also accepted: --overriding, --override.
      --access <level>      Only members at this accessibility: public, protected, internal, private, 'protected internal', or 'private protected'.
                            Also accepted: --accessibility, --visibility.
      --inherited           Also list what the base types declare, each row saying which type declares it.
                            Also accepted: --with-inherited, --include-base.
  -n, --limit <n>           How many members to return, at most. Left out, every one is returned. Default: every one.
                            Also accepted: --max-results, --count, --top, --rows, --num, --head.

Global options (every command takes these, written after the command):
      --snapshot <name>  Query this named snapshot instead of the one that would be picked automatically. An explicit choice always wins over auto-detection.
                         Also accepted: --snap, --env.
      --db <path>        Query the snapshot database at this path directly, bypassing the registry.
                         Also accepted: --database, --snapshot-path.
      --json             Emit machine-readable JSON. Anything the text output would have said in prose moves into a 'notes' array, so nothing is lost. The command's own table key is always present — an empty array when nothing matched, never a missing key. A note that reports a count also carries 'shown' and 'total' as numbers, so the figures never have to be parsed back out of its text; 'total' is null when only a lower bound is known, and both keys are absent on notes that are not counts.
      --config <path>    Use this config file instead of the default one.

--json keys (besides 'notes'):
  members  one row per member: assembly, type, kind, member, signature, static, virtual, abstract, override, accessibility.

Examples:
  rimsearcher members Verse.ThingComp
  rimsearcher members Verse.Pawn --virtual
  rimsearcher members Verse.Pawn --name Faction --inherited
  rimsearcher members RimWorld.GenRecipe --member-kind method --static

$ rimsearcher callers --help
Usage: rimsearcher callers <symbol> [options]

Find the methods that call a given method, or the ones it calls.

Answered from a call-graph table built by 'rimsearcher sources sync', one per source tree. A tree without one is not searched, and the output names those trees — a caller living there would not appear below.

The recorded target is the method named at the call site. A callvirt names the base method even when the object it runs on is a subclass, so calls that dispatch to an override at run time are counted against the base — 'rimsearcher types <type> --derived' finds the overrides themselves.

--callees turns it around and lists what the named method calls.

Arguments:
  <symbol>  The method: 'Verse.Pawn.Tick', 'Verse.Pawn::Tick', 'Verse.ThingDef..ctor'.

Options:
      --callees        List what this method calls instead of what calls it.
                       Also accepted: --calls, --outgoing, --reverse.
      --source <tree>  Only count call sites in this source tree. Both ends are still named from every tree, so the answer is narrowed, not blinded.
                       Also accepted: --tree, --from-tree.
  -n, --limit <n>      How many callers to return, at most. Left out, every one is returned. Default: every one.
                       Also accepted: --max-results, --count, --top, --rows, --num, --head.

Global options (every command takes these, written after the command):
      --snapshot <name>  Query this named snapshot instead of the one that would be picked automatically. An explicit choice always wins over auto-detection.
                         Also accepted: --snap, --env.
      --db <path>        Query the snapshot database at this path directly, bypassing the registry.
                         Also accepted: --database, --snapshot-path.
      --json             Emit machine-readable JSON. Anything the text output would have said in prose moves into a 'notes' array, so nothing is lost. The command's own table key is always present — an empty array when nothing matched, never a missing key. A note that reports a count also carries 'shown' and 'total' as numbers, so the figures never have to be parsed back out of its text; 'total' is null when only a lower bound is known, and both keys are absent on notes that are not counts.
      --config <path>    Use this config file instead of the default one.

--json keys (besides 'notes'):
  calls  one row per calling method and target pair: tree, from_assembly, from_type, from_member, to_assembly, to_type, to_member, call_sites.

Examples:
  rimsearcher callers Verse.Pawn.Kill
  rimsearcher callers RimWorld.ThoughtUtility.GiveThoughtsForPawnOrganHarvested --source vethara
  rimsearcher callers Verse.Pawn.Tick --callees

$ rimsearcher il --help
Usage: rimsearcher il <symbol> [options]

Disassemble a method to IL.

Name the method as 'Verse.Pawn.Tick', 'Verse.Pawn::Tick', or 'M:Verse.Pawn.Tick' — all three work, as does a bare type name plus member. A property is written by its C# name: 'Faction' finds the get_Faction and set_Faction that IL actually holds. A constructor is '.ctor'. Every overload of the name is disassembled, each under its own signature.

Iterators and async methods keep almost nothing in the method itself — the instructions live in a compiler-generated state machine, and that is what a transpiler has to match. This command says so and names the method to ask for instead; --state-machine goes there directly.

Page with --from/--to, which are IL offsets, not line numbers. A method with no body at all (abstract, extern, an engine intrinsic) is reported as such rather than as an empty result.

Arguments:
  <symbol>  The method to disassemble: 'Verse.Pawn.Tick', 'RimWorld.Need.CurLevel', 'Verse.ThingDef..ctor'.

Options:
      --source <tree>  Which decompiled source tree to read. Omit to read them all.
                       Also accepted: --tree, --from-tree.
      --from <offset>  Start at this IL offset. Decimal, or hex with an 0x prefix. The method header is always shown — instructions cannot be read without knowing the locals.
                       Also accepted: --start-offset, --offset-from.
      --to <offset>    Stop after this IL offset.
                       Also accepted: --end-offset, --offset-to.
      --state-machine  Go straight to the state machine's MoveNext when the named method is an iterator or an async method. Without it the method itself is shown and the state machine is named.
                       Also accepted: --movenext, --follow.
  -n, --limit <n>      How many lines to return, at most. Left out, every one is returned. Default: every one.
                       Also accepted: --max-results, --count, --top, --rows, --num, --head.

Global options (every command takes these, written after the command):
      --snapshot <name>  Query this named snapshot instead of the one that would be picked automatically. An explicit choice always wins over auto-detection.
                         Also accepted: --snap, --env.
      --db <path>        Query the snapshot database at this path directly, bypassing the registry.
                         Also accepted: --database, --snapshot-path.
      --json             Emit machine-readable JSON. Anything the text output would have said in prose moves into a 'notes' array, so nothing is lost. The command's own table key is always present — an empty array when nothing matched, never a missing key. A note that reports a count also carries 'shown' and 'total' as numbers, so the figures never have to be parsed back out of its text; 'total' is null when only a lower bound is known, and both keys are absent on notes that are not counts.
      --config <path>    Use this config file instead of the default one.

--json keys (besides 'notes'):
  il  one row per disassembled method: assembly, type, member, signature, bodyless, first_offset, last_offset, shown_from, shown_to, lines.

Examples:
  rimsearcher il Verse.Pawn.Tick
  rimsearcher il Verse.Pawn.GetGizmos --state-machine
  rimsearcher il RimWorld.MainTabWindow_Research.DrawProjectInfo --from 0x160 --to 0x3e0

